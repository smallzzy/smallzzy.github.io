what the kernel need to provide?

1. c library: provide a POSIX API
2. linux headers: for c library and device driver

cross compile

yocto, petalinux
buildroot

ABI, calling convention
EABI / EABIHF
